# VK Music Saver

<div align="center">

![VK Music Saver](https://img.shields.io/badge/VK-Music%20Saver-0077FF?style=for-the-badge&logo=vk&logoColor=white)
![React](https://img.shields.io/badge/React-18-61DAFB?style=for-the-badge&logo=react&logoColor=black)
![FastAPI](https://img.shields.io/badge/FastAPI-0.100+-009688?style=for-the-badge&logo=fastapi&logoColor=white)
![MongoDB](https://img.shields.io/badge/MongoDB-6+-47A248?style=for-the-badge&logo=mongodb&logoColor=white)

**Веб-приложение для скачивания музыки из ВКонтакте.**

---

**Скачивайте плейлисты, отдельные треки или всю музыкальную библиотеку одним кликом!**

---


> **Поддержать донатом:** [клик 🍄](https://yoomoney.ru/fundraise/1CV7BFDQ964.250923)

Спасиба <3


</div>

---

## ✨ Возможности

- 🎵 **Скачивание плейлистов** — скачайте любой плейлист VK по ссылке
- 🎤 **Скачивание треков** — скачайте отдельный трек по ссылке
- 📚 **Вся библиотека** — скачайте всю свою музыку одним кликом
- 📦 **Массовое скачивание** — несколько плейлистов за раз
- 🏷️ **ID3 теги** — автоматическое добавление метаданных (исполнитель, название, обложка)
- 📝 **Тексты песен** — встраивание текстов в MP3 файлы
- 🌐 **Поддержка прокси** — HTTP, SOCKS5, VLESS (через Xray)
- 📤 **Автозагрузка на TempShare** — готовые ссылки для скачивания
- 📊 **Прогресс в реальном времени** — отслеживание процесса загрузки
- ⚡ **Параллельная загрузка** — до 8 треков одновременно
- 🔄 **Автоматическое разделение** — архивы >2 ГБ делятся на части
- 💾 **Чанковая загрузка** — для больших библиотек (>1 ГБ) треки загружаются частями для экономии места на диске

---

## 🚀 Быстрый старт

### Требования

- Python 3.9+
- Node.js 18+
- Yarn
- MongoDB 6+
- Xray-core (опционально, для VLESS прокси)

### Установка и запуск

```bash
# Клонируйте репозиторий
git clone https://github.com/kage-kao/VK-Music-Saver.git
cd VK-Music-Saver

# Запустите автоматическую установку
chmod +x deploy.sh
./deploy.sh
```

Скрипт автоматически:
1. Установит Xray для VLESS прокси
2. Установит зависимости Python и Node.js
3. Создаст конфигурационные файлы
4. Запустит бэкенд на `http://localhost:8001`
5. Запустит фронтенд на `http://localhost:3000`

### Ручная установка

```bash
# Бэкенд
cd backend
pip install -r requirements.txt
cp .env.example .env  # настройте .env
uvicorn server:app --host 0.0.0.0 --port 8001 --reload

# Фронтенд (в новом терминале)
cd frontend
yarn install
yarn start
```

### Установка Xray (для VLESS прокси)

Xray требуется только если вы планируете использовать VLESS прокси:

```bash
# Linux x64
curl -sL https://github.com/XTLS/Xray-core/releases/latest/download/Xray-linux-64.zip -o xray.zip
unzip xray.zip -d /usr/local/bin/
chmod +x /usr/local/bin/xray
rm xray.zip
```

> ⚠️ Без установленного Xray VLESS прокси работать не будут. HTTP и SOCKS5 прокси работают без него.

---

## 📖 Использование

### 1. Авторизация

Приложение использует токен VK с правами доступа к аудио. Получить токен можно:
- Через приложения Kate Mobile, VK Admin
- [Инструкция по получению токена](https://telegra.ph/Poluchenie-klyucha-tokena-API-02-18)

### 2. Скачивание

**Плейлист:**
- Вставьте ссылку вида `https://vk.com/music?z=audio_playlist-2001234567_12345678`

**Трек:**
- Вставьте ссылку вида `https://vk.com/audio-2001234567_456239017`

**Моя музыка:**
- Нажмите "Моя музыка" → "Скачать мою музыку"

### 3. Настройки прокси

Если сервер находится за пределами России, некоторые треки могут быть недоступны. Настройте российский прокси:

- **HTTP**: `ip:port` или `user:pass@ip:port`
- **SOCKS5**: `ip:port` или `user:pass@ip:port`
- **VLESS**: полный URI вида `vless://uuid@server:port?...` (требует Xray)

---

## 🛠️ Конфигурация

### Backend (.env)

```env
MONGO_URL="mongodb://localhost:27017"
DB_NAME="vk_music_saver"
CORS_ORIGINS="*"
```

### Frontend (.env)

```env
REACT_APP_BACKEND_URL=http://localhost:8001
WDS_SOCKET_PORT=3000
```

---

## 📦 Структура проекта

```
vk-music-saver/
├── backend/
│   ├── server.py          # FastAPI приложение
│   ├── requirements.txt   # Python зависимости
│   └── .env               # Переменные окружения
├── frontend/
│   ├── src/
│   │   ├── App.js         # Главный React компонент
│   │   ├── App.css        # Стили
│   │   └── ...
│   ├── package.json       # Node зависимости
│   └── .env               # Переменные окружения
├── deploy.sh              # Скрипт развёртывания
├── README.md              # Документация
└── DOCUMENTATION.md       # Техническая документация
```

---

## 🔗 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/vk/token-login` | Авторизация по токену |
| POST | `/api/vk/logout` | Выход из сессии |
| POST | `/api/download/start` | Скачать плейлист |
| POST | `/api/download/track` | Скачать трек |
| POST | `/api/download/my-music` | Скачать всю музыку |
| POST | `/api/download/multi` | Скачать несколько плейлистов |
| GET | `/api/download/status/{id}` | Статус задачи |
| POST | `/api/download/cancel/{id}` | Отмена загрузки |
| DELETE | `/api/download/{id}` | Удалить задачу из истории |
| GET | `/api/download/history/{session_id}` | История загрузок |
| GET | `/api/download/active/{session_id}` | Активные загрузки |
| GET | `/api/proxies` | Список прокси |
| POST | `/api/proxies` | Добавить прокси |
| POST | `/api/proxies/{id}/toggle` | Вкл/выкл прокси |
| POST | `/api/proxies/{id}/check` | Проверить прокси |
| DELETE | `/api/proxies/{id}` | Удалить прокси |

---

## ⚠️ Важные ограничения

- **TempShare**: максимальный размер файла 2 ГБ (автоматическое разделение)
- **Чанки**: при размере > 1 ГБ создаются промежуточные архивы
- **Ссылки**: действительны 7 дней
- **Имена файлов**: ограничены 200 символами для совместимости с файловыми системами
- **Региональные ограничения**: некоторые треки доступны только из России
- **VLESS прокси**: требуют установленный Xray (`/usr/local/bin/xray`)

---

## 📝 Лицензия

MIT License

---

## 👤 Разработано:

- KageKao
